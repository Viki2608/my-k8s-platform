apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: python-app-ingress-nginx                    
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/Viki2608/python-app.git'
    targetRevision: ingress-nginx  # The branch name
    path: gitops                   # The directory inside that branch
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: ingress-nginx                    
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true